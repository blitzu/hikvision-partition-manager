---
wave: 3
depends_on: [03-02-PLAN.md]
files_modified:
  - app/main.py
  - app/partitions/service.py
  - app/partitions/routes.py
  - app/locations/routes.py
  - app/nvrs/routes.py
  - tests/test_partitions.py
autonomous: true
---

# Plan: Dashboard & API Polish

Implement the summary dashboard and perform a final consistency check of all API endpoints to ensure they meet the phase requirements.

## Tasks

<task id="03-03-01" requirement="API-08, PART-02">
Implement `GET /api/dashboard` route in `app/partitions/routes.py` and its corresponding service logic in `service.py`. This endpoint aggregates:
- List of partitions with state
- Dynamic disarmed duration calculation
- Alert flags (overdue)
- Sorted by active state (error/partial/disarmed first)
</task>

<task id="03-03-02" requirement="API-02, API-03, API-09">
Review and polish existing endpoints in `locations`, `nvrs`, and `cameras` to ensure they strictly follow `APIResponse` and return correct error codes (e.g., 422 for validation errors via Pydantic).
</task>

<task id="03-03-03" requirement="API-01, API-08, API-09">
Complete final tests in `tests/test_partitions.py` and ensure the dashboard returns the expected summaries and sorts correctly.
</task>

## Verification Criteria

### Automated Tests
- `pytest` (full suite)
- Verify `GET /api/dashboard` returns expected partition summary.
- Verify `GET /api/dashboard` correctly calculates alert flag and duration.
- Ensure all API endpoints return `success, data, error` structure.

### must_haves
- [ ] Dashboard calculates disarmed duration at request time.
- [ ] Dashboard alert flag is active if rearm time is passed.
- [ ] Active/non-armed partitions appear first on dashboard.
- [ ] All routes use `APIResponse` envelope consistently.
