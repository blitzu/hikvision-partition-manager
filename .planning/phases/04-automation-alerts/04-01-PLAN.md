---
phase: 04-automation-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/jobs/__init__.py
  - app/jobs/scheduler.py
  - app/jobs/auto_rearm.py
  - app/main.py
  - app/partitions/service.py
  - tests/test_jobs_auto_rearm.py
autonomous: true
requirements:
  - JOB-01
  - ALRT-01

must_haves:
  truths:
    - "APScheduler starts on app startup and shuts down cleanly on exit"
    - "A partition disarmed with auto_rearm_minutes set has a job scheduled in APScheduler at scheduled_rearm_at"
    - "When the auto-rearm job fires, arm_partition() is called with performed_by='system:auto_rearm'"
    - "Manually arming a partition before the scheduled time removes its job from the scheduler"
    - "Auto-rearm completion fires the auto_rearmed webhook payload"
  artifacts:
    - path: "app/jobs/__init__.py"
      provides: "Package init"
    - path: "app/jobs/scheduler.py"
      provides: "Shared AsyncScheduler instance and lifespan helpers"
    - path: "app/jobs/auto_rearm.py"
      provides: "auto_rearm_job function and schedule_rearm / cancel_rearm helpers"
    - path: "tests/test_jobs_auto_rearm.py"
      provides: "Unit tests for schedule/cancel/fire logic"
  key_links:
    - from: "app/main.py lifespan"
      to: "app/jobs/scheduler.py"
      via: "scheduler.start() / scheduler.shutdown() in lifespan"
    - from: "app/partitions/service.py disarm_partition()"
      to: "app/jobs/auto_rearm.py schedule_rearm()"
      via: "called after DB commit when auto_rearm_minutes is set"
    - from: "app/partitions/service.py arm_partition()"
      to: "app/jobs/auto_rearm.py cancel_rearm()"
      via: "called unconditionally before DB commit"
    - from: "app/jobs/auto_rearm.py auto_rearm_job()"
      to: "app/partitions/service.py arm_partition()"
      via: "direct async call with own DB session"
---

<objective>
Set up APScheduler with PostgreSQL job store, implement the auto-rearm background job, and wire job scheduling/cancellation into the disarm/arm service functions.

Purpose: Partitions disarmed with auto_rearm_minutes must rearm themselves at the scheduled time without any manual trigger, and manual arm must cancel the pending job.
Output: app/jobs/ module, scheduler integrated into FastAPI lifespan, auto-rearm job with webhook, tests for schedule/cancel/fire behavior.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation-alerts/04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/core/database.py:
```python
async_session_factory: async_sessionmaker  # use async_session_factory() to get a session in jobs
engine: AsyncEngine                         # used in SQLAlchemyJobStore URL
```

From app/partitions/service.py:
```python
async def arm_partition(
    partition_id: uuid.UUID,
    armed_by: str,          # pass 'system:auto_rearm' for auto-rearm jobs
    db: AsyncSession,
) -> ArmResponse: ...

async def disarm_partition(...) -> DisarmResponse: ...
# After DB commit, if partition.auto_rearm_minutes: schedule_rearm(partition_id, scheduled_rearm_at)
# In arm_partition, before state update: cancel_rearm(partition_id)
```

From app/partitions/models.py:
```python
class PartitionState:
    scheduled_rearm_at: datetime | None
    # arm_partition() already sets scheduled_rearm_at = None
```

From app/nvrs/models.py:
```python
class NVRDevice:
    id: uuid.UUID
    name: str
    status: str  # 'online' | 'offline' | 'unknown'
    last_seen_at: datetime | None
    ip_address: str
    port: int
    username: str
    password_encrypted: str
```

From app/main.py:
```python
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncGenerator[None, None]:
    await asyncio.to_thread(_run_migrations)
    # ADD: start scheduler here, reconcile missed jobs
    yield
    await engine.dispose()
    # ADD: shutdown scheduler here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add APScheduler dependency and create app/jobs/ module with scheduler and auto-rearm job</name>
  <files>
    pyproject.toml,
    app/jobs/__init__.py,
    app/jobs/scheduler.py,
    app/jobs/auto_rearm.py,
    tests/test_jobs_auto_rearm.py
  </files>
  <behavior>
    - schedule_rearm(partition_id, run_at): adds a DateTrigger job to scheduler with id="rearm:{partition_id}"; if job already exists, replaces it
    - cancel_rearm(partition_id): removes job "rearm:{partition_id}" from scheduler; no-op if not found
    - auto_rearm_job(partition_id_str): opens own AsyncSession via async_session_factory(), calls arm_partition(partition_id, 'system:auto_rearm', db), then fires auto_rearmed webhook as asyncio.create_task; session closed after commit regardless of outcome
    - deliver_webhook(payload): async function, POST to settings.ALERT_WEBHOOK_URL with 5s timeout; retries on failure with delays [1, 5, 15] seconds (3 retries); non-blocking — caller wraps in asyncio.create_task; logs failures, never raises
    - Webhook payload for auto_rearmed: {"type": "auto_rearmed", "partition_id": str(partition_id), "partition_name": name}
    - Tests use monkeypatching / mocking — no real DB or APScheduler instance needed for unit tests; test schedule_rearm adds correct job_id, cancel_rearm removes it, auto_rearm_job calls arm_partition with correct args
  </behavior>
  <action>
    1. Add `"apscheduler[asyncpg]>=4.0"` to pyproject.toml dependencies. Run `pip install -e ".[test]"` (or `pip install apscheduler[asyncpg]`) to install.

    2. Create `app/jobs/__init__.py` as empty file.

    3. Create `app/jobs/scheduler.py`:
       - Import `AsyncScheduler` from `apscheduler`
       - Import `SQLAlchemyDataStore` from `apscheduler.datastores.sqlalchemy`
       - Import `engine` from `app.core.database`
       - Create a module-level `scheduler: AsyncScheduler` instance using SQLAlchemyDataStore backed by the existing `engine`
       - Expose `async def start_scheduler()` and `async def shutdown_scheduler()` helpers

    4. Create `app/jobs/auto_rearm.py`:
       - `deliver_webhook(payload: dict) -> None`: async function that POST to `settings.ALERT_WEBHOOK_URL` (import from `app.core.config`). Use `httpx.AsyncClient` with 5s timeout. Retry loop: attempts = [0, 1, 2, 3] with delays [0, 1, 5, 15] before each retry except first. On all retries exhausted, log error and return silently. Never raise.
       - `schedule_rearm(partition_id: uuid.UUID, run_at: datetime) -> None`: calls `await scheduler.add_job(auto_rearm_job, DateTrigger(run_date=run_at), id=f"rearm:{partition_id}", replace_existing=True, kwargs={"partition_id_str": str(partition_id)})` — use APScheduler 4.x API (job function passed directly, trigger as positional arg, kwargs dict for job args)
       - `cancel_rearm(partition_id: uuid.UUID) -> None`: calls `await scheduler.remove_job(f"rearm:{partition_id}")` wrapped in try/except to swallow JobLookupError if not found
       - `async def auto_rearm_job(partition_id_str: str) -> None`: parse UUID, open session via `async with async_session_factory() as db`, call `arm_partition(partition_id, "system:auto_rearm", db)`, get partition name for webhook payload, fire `asyncio.create_task(deliver_webhook({...}))`. Import `async_session_factory` from `app.core.database` and `arm_partition` from `app.partitions.service` (deferred import inside function body to avoid circular imports).

    5. Write `tests/test_jobs_auto_rearm.py` with pytest-asyncio tests:
       - Test `schedule_rearm` adds job with correct id to a mock scheduler
       - Test `cancel_rearm` calls remove_job with correct id; no-op when job missing
       - Test `auto_rearm_job` calls arm_partition with performed_by='system:auto_rearm'
       - Test `deliver_webhook` retries on failure, succeeds on second attempt, gives up after 3 retries
       - Use `unittest.mock.AsyncMock` and `monkeypatch` to avoid real DB/HTTP
  </action>
  <verify>
    <automated>cd /root/hikvision-partition-manager && python -m pytest tests/test_jobs_auto_rearm.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_jobs_auto_rearm.py pass. app/jobs/scheduler.py and app/jobs/auto_rearm.py exist with correct exports.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scheduler into FastAPI lifespan and hook schedule/cancel into disarm/arm service</name>
  <files>
    app/main.py,
    app/partitions/service.py
  </files>
  <action>
    1. Update `app/main.py` lifespan:
       - Import `start_scheduler` and `shutdown_scheduler` from `app.jobs.scheduler`
       - After `await asyncio.to_thread(_run_migrations)`, call `await start_scheduler()`
       - After migrations complete: reconcile missed jobs — query all `PartitionState` rows where `scheduled_rearm_at IS NOT NULL` and `state = 'disarmed'`; for each, check if job `"rearm:{partition_id}"` exists in scheduler; if not, call `schedule_rearm(partition_id, scheduled_rearm_at)`. If `scheduled_rearm_at` is already in the past, APScheduler fires immediately (default misfire behavior — do not suppress).
       - In the shutdown block (after `yield`), call `await shutdown_scheduler()` before `await engine.dispose()`

    2. Update `app/partitions/service.py` `disarm_partition()`:
       - Import `schedule_rearm` and `cancel_rearm` from `app.jobs.auto_rearm` at top of file (or deferred inside function — use module-level import)
       - After `await db.commit()` (step 11), if `state.scheduled_rearm_at` is not None: call `await schedule_rearm(partition_id, state.scheduled_rearm_at)`

    3. Update `app/partitions/service.py` `arm_partition()`:
       - After loading the partition (step 1), call `await cancel_rearm(partition_id)` — do this before any DB writes, so even if arm fails partway, the job is already cancelled
       - Note: `arm_partition()` already sets `state.scheduled_rearm_at = None` in step 6 — this is correct per ARM-05

    4. Verify no circular import: `app/jobs/auto_rearm.py` imports `arm_partition` inside the function body (deferred), not at module level. `app/partitions/service.py` imports `schedule_rearm`/`cancel_rearm` at module level from `app.jobs.auto_rearm` — this is safe because `auto_rearm.py` does not import from `service.py` at module level.
  </action>
  <verify>
    <automated>cd /root/hikvision-partition-manager && python -c "from app.main import app; print('import OK')" && python -m pytest tests/ -x -q --ignore=tests/test_jobs_auto_rearm.py 2>&1 | tail -10</automated>
  </verify>
  <done>App imports cleanly. All pre-existing tests still pass. Lifespan starts/stops scheduler. disarm_partition schedules rearm job; arm_partition cancels it.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passes all tests including new test_jobs_auto_rearm.py
- `python -c "from app.main import app"` completes without import errors
- `app/jobs/scheduler.py` exports a module-level `AsyncScheduler` instance backed by SQLAlchemyDataStore
- `disarm_partition()` calls `schedule_rearm()` when `auto_rearm_minutes` is set
- `arm_partition()` calls `cancel_rearm()` unconditionally
</verification>

<success_criteria>
- APScheduler 4.x installed and importable
- auto_rearm_job calls arm_partition with performed_by='system:auto_rearm'
- deliver_webhook retries 3x with [1, 5, 15]s delays then logs and gives up
- Scheduler start/stop wired into FastAPI lifespan
- Startup reconciliation re-registers any jobs missing from the store
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation-alerts/04-01-SUMMARY.md` using the summary template.
</output>
